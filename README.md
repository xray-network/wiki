# XRAY Wiki

XRAY/Network ecosystem knowledge base, built with [Rspress](https://rspress.dev/).

```sh
npm install
npm run dev
```

The development server runs at `http://localhost:10005`. Build static output with `npm run build`; Rspress writes it to `build` for deployment through the `wiki-xray-network-docs` Cloudflare Worker.
